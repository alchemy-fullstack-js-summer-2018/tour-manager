// const { assert } = require('chai');
// const request = require('./request')
// const { dropCollection } = require('./db');

// describe('Tour API', () => {

//     beforeEach(() => dropCollection('stops'));

//     function save(stop) {
//         return request
//             .post('/api/stops')
//             .sen(stop)
//             .then(checkOk)
//             .then(({ body }) => body);
//     }

// });